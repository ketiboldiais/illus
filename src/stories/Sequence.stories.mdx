import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
} from "@storybook/addon-docs/blocks";
import { Stack } from "../components/Stack/Stack";

<Meta
	title="Stacks"
	parameters={{
		viewMode: "docs",
		previewTabs: {
			canvas: { hidden: true },
		},
	}}
	component={Stack}
/>

export const Template = (args) => <Stack {...args} />;

# Stacks

Another simple data structure is the stack:

<Story
	name="Stacks"
	args={{
		data: ["f()", "g()", "h()", "i()", "j()", "k()"],
	}}>
	{Template.bind({})}
</Story>

<ArgsTable story="Stacks" />
