import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
} from "@storybook/addon-docs/blocks";

import { Source } from "@storybook/addon-docs";

import { BinaryTree } from "../components/BinaryTree/BinaryTree";

import { Sequence } from "../components/Sequence/Sequence";

import { LinkedList } from "../components/LinkedList/LinkedList";

<Meta
	title="Sequence"
	parameters={{
		viewMode: "docs",
		previewTabs: {
			canvas: { hidden: true },
		},
	}}
	argTypes={{
		data: {
			description: "An array of arrays of object literals or primitives",
			table: {
				type: {
					summary: "[ {val: <number | string>} | number | string ]",
				},
			},
		},
	}}
	component={BinaryTree}
/>

export const Template = (args) => <Sequence {...args} />;

# Sequence

Binary trees are used extensively in computer science. The `BinaryTree`
module provides a component for rendering binary trees.

<Story
	name="Sequence"
	args={{
		data: [1,2,3,4,5],
	}}>
	{Template.bind({})}
</Story>

<ArgsTable story="Sequence" />
