import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
} from "@storybook/addon-docs/blocks";
import { CircularList } from "../components/CircularList/CircularList";

<Meta
	title="Circular List"
	parameters={{
		viewMode: "docs",
		previewTabs: {
			canvas: { hidden: true },
		},
	}}
	component={CircularList}
/>

export const Template = (args) => <CircularList {...args} />;

# Circular Lists

Illus renders bar plots with a matrix of arrays.

<Story
	name="Circular List"
	args={{
		data: [2, 3, 9, 11, 17, 20],
	}}>
	{Template.bind({})}
</Story>

<ArgsTable story="Circular List" />
