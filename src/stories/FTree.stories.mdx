import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
} from "@storybook/addon-docs/blocks";
import { FTree } from "../components/FTree/FTree";

<Meta
	title="Force-directed Trees"
	parameters={{
		viewMode: "docs",
		previewTabs: {
			canvas: { hidden: true },
		},
	}}
	component={FTree}
/>

export const Template = (args) => <FTree {...args} />;

# Force-directed Trees

The `FTree` module creates a force-directed tree. The data structure passed
is an array of arrays — the same structure used in the `Tree` module. The
first element in the nested array is the node, and the second element is
its parent.

<Story
	name="Force-directed Trees"
	args={{
		data: [
			[1, ""],
			[2, 1],
			[3, 1],
			[4, 1],
			[5, 2],
			[6, 2],
			[7, 3],
			[8, 3],
			[9, 4],
			[10, 4],
		],
	}}>
	{Template.bind({})}
</Story>

<ArgsTable story="Force-directed Trees" />
