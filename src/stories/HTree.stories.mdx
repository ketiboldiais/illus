import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
} from "@storybook/addon-docs/blocks";
import { HTree } from "../components/HTree/HTree";

<Meta
	title="Horizontal Trees"
	parameters={{
		viewMode: "docs",
		previewTabs: {
			canvas: { hidden: true },
		},
	}}
	component={HTree}
/>

export const Template = (args) => <HTree {...args} />;

# Horizontal Trees

Trees can also be viewed horizontally, providing a helpful way of looking
at call traces:

<Story
	name="Horizontal Trees"
	args={{
		data: [
			[`p(0)`],
			[`p(0)`, `8`, `p(1)`, `p(2)`],
			[`p(1)`, `3`, `p(3)`, `p(4)`],
			[`p(2)`, `4`, `p(5)`, `p(6)`],
		],
	}}>
	{Template.bind({})}
</Story>

<ArgsTable story="Horizontal Trees" />
