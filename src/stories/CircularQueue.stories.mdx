import {
	Meta,
	Story,
	Canvas,
	ArgsTable,
} from "@storybook/addon-docs/blocks";
import { CircularQueue } from "../components/CircularQueue/CircularQueue";

<Meta
	title="Circular Queue"
	parameters={{
		viewMode: "docs",
		previewTabs: {
			canvas: { hidden: true },
		},
	}}
	component={CircularQueue}
/>

export const Template = (args) => <CircularQueue {...args} />;

# Circular Queues

A variant of the queue is a circular queue:

<Story
	name="Circular Queue"
	args={{
		data: [5, 8, 9, 7, 3],
	}}>
	{Template.bind({})}
</Story>

<ArgsTable story="Circular Queue" />
